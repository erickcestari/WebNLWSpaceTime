/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', ''], // add your server ip to provide image
  },
}

module.exports = nextConfig
